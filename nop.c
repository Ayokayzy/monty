#include "monty.h"

/**
 * op_nop - does nothing
 * followed by a new line.
 * @stack: the stack
 * @line_number: line number
 */
void op_nop(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
